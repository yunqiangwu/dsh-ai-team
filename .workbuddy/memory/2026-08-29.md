# 2026-08-29

## dsh-ai-team 重构：提升可读性与可维护性 + 注释中文化

在 `/Users/yunke/WorkBuddy/dsh-autopilot/`（dsh-ai-team 插件）做代码质量重构。

**目标**：提升可读性/可维护性；规范文档加规则「代码注释尽量用中文」。

**改动**：
1. **抽离 GitHub REST 逻辑 → `src/github.ts`（新文件）**：从 service.ts 抽出 `githubRepoSlug` / `upsertPullRequest` / `checkRunStatus`，service 不再掺 HTTP 细节。`PullRequestUpsert` = `{created:true;url:string|null} | {created:false}`。
2. **消除 service.ts 重复逻辑与魔法值**：删去契约路径解析、任务时间戳刷新、契约状态回写、assignee 释放等重复逻辑，消掉魔法值（`CONTRACT_BODY_LIMIT=2000`、`memberBranch`/`shortId` 助手）。
3. **全量注释中文化**：service.ts / git.ts / deploy.ts / escalate.ts / gates.ts / team.ts / profile.ts / view.ts / projection.ts / events.ts / notification.ts 及并行任务已处理的 index.ts / client/* / bootstrap.ts / cache.ts / preset.ts / roles.ts / secrets.ts / tools.ts。**只翻注释，不动标识符、字符串字面量、运行时消息、i18n 字典、tool 描述**。
4. **AGENTS.md 更新**：目录地图补 `profile.ts`（项目画像适配器）/ `cache.ts`（构建缓存共享）/ `github.ts`（GitHub REST 适配层）/ `client/contract.ts`；代码风格改为「标识符英文、注释尽量中文」；「改一处要连带改哪儿」补 `profile.ts` 与远端平台/PR 能力两行。

**排错**：
- `updateContractStatus` owner 参数默认 `null` 会在每次状态更新抹掉 owner → 改 optional（`owner?: string | null`），仅在显式传入时 patch；`reopenTask` 显式传 `null` 清 owner。
- `syncContracts` 原写法会回写契约文件 → 改为只在内存更新 state，避免循环写（契约文件本就是权威源）。
- 首版 subagent 并行翻译遭遇 429 额度限制 → 改为手动逐文件翻译。
- 删除未使用的 `BOARD_FILE` 常量。

**质量门全绿**：`pnpm typecheck` 0 error / `pnpm lint` 0 warning / `pnpm test` 51 tests 全绿 / `pnpm build` 成功 / `pnpm pack --dry-run` 产物含 lib+preset+README+LICENSE+cordis.patch.yml。
